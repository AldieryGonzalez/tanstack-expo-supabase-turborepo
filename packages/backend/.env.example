# Deployment used by `npx convex dev` (e.g. dev:your-team-xxx)
CONVEX_DEPLOYMENT=your:deployment-here

# Local Convex dev URLs (convex dev prints these)
CONVEX_URL=http://127.0.0.1:3210
CONVEX_SITE_URL=http://127.0.0.1:3211

# --- Set these in Convex Dashboard (Settings > Environment Variables) ---
# BETTER_AUTH_SECRET=    # run: openssl rand -base64 32
# SITE_URL=              # e.g. https://auth-local.your-domain.com (tunnel mode) or http://127.0.0.1:3211 (direct local)
# GOOGLE_CLIENT_ID=      # for Google sign-in
# GOOGLE_CLIENT_SECRET=
# TRUSTED_ORIGINS=       # optional, comma-separated extra web origins (e.g. https://web-local.your-domain.com)
# MOBILE_APP_SCHEME=     # optional, defaults to "myapp"
